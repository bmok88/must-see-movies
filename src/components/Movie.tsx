import React, { FunctionComponent } from 'react';
import { Link } from '@reach/router';

export interface MovieType {}

const Movie: FunctionComponent<MovieType> = () => {
    retrun(<Link></Link>);
};

export default Movie;
